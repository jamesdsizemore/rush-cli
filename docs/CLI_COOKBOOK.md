# CLI cookbook

Use [Examples](EXAMPLES.md), [Recipe book](RECIPE_BOOK.md), and the authoritative [CLI reference](reference/cli-reference.md). Key patterns are `rush COMMAND PATH`, add `--json` for automation, use `format --check` for non-mutating verification, and set `--log-level debug` only when diagnosing stderr logs.
