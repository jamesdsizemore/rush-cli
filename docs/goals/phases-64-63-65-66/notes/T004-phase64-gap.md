# T004 — Phase 64 true remaining gap (Scout findings)

Source read in full: docs/phase-plans/phase-64-runtime-correctness-and-safe-execution-plan.md (206 lines, P64-00..P64-20). Evidence file read in full: docs/phase-plans/phase-64-implementation-evidence.md (43 lines) — only documents P64-00 done, P64-01 done, P64-02 provisional; states P64-03..P64-20 "remain planned until their own RED, GREEN, and acceptance evidence is added." That statement is FALSE for several packets — real source implementation exists beyond what evidence.md admits.

## Packet status (source-verified, not doc-trusted)

- P64-00: done. evidence.md:9-11 cites passing tests/test_sync_docs.py (13 passed) and scripts/sync_docs.py --check ran clean at doc-freeze time. Re-ran `uv run python scripts/sync_docs.py --check` live this session: now exits 1 with 56+ findings (see contract drift below + stale doc shas: docs/CLI_REFERENCE.md, docs/MCP_REFERENCE.md, docs/SAFETY.md, docs/safety/permissions.md, docs/safety/safety-overview.md, docs/vibecoding/the-vibecoder-workflow.md, docs/phase-plans/phase-64-implementation-evidence.md itself — all "stale sha256", meaning docs changed since the coverage receipt was generated and P64-00's checker is not currently green). Status: implemented-but-currently-failing-its-own-check (docs drifted post-evidence-freeze).
- P64-01: done per evidence.md:15-27, not independently re-verified against source hash (out of scope to re-derive SHA-256; test suite exists: tests/test_fix.py).
- P64-02: provisional per evidence.md:29-39 (explicit self-admission, "does not mark P64-02 complete"). src/rush/tools/ship/cleaner.py exists; register_owned_artifact is real and consumed by P64-08/09/10/11 (see below) — so P64-02's core primitive is implemented and already load-bearing for later packets even though its own packet evidence is provisional.
- P64-03: evidence.md silent (not listed as done anywhere). tests/test_agent_governance.py::test_sync_rejects_escape_before_any_write and ::test_sync_second_write_failure_restores_first both EXIST and PASS live (`uv run python -m pytest tests/test_agent_governance.py -q -k "escape or second_write_failure"` -> 7 passed, 7 deselected). Status: implemented_but_unevidenced — behavior works, evidence.md never updated to record it.
- P64-04: partially implemented, genuinely incomplete on registration. src/rush/tools/patch_apply.py exists (644 lines, PatchApplyTool). src/rush/cli.py:1324 defines `patch_apply_cmd` but it is NOT wired to any click group/command decorator found by grep (`patch_apply_cmd|@.*command.*patch|add_command.*patch` in cli.py matches only the def line) — sync_docs confirms: `contracts.cli: missing registered command rush patch apply` (live check). governance/public_operations.py:211 and governance/public-operations.toml:434 both reference "rush_patch_apply", but src/rush/mcp.py has ZERO literal occurrence of "rush_patch_apply"/"PatchApplyTool" — sync_docs confirms: `contracts.mcp: missing registered command rush_patch_apply`. src/rush/patch/{sandbox,applier,promoter}.py show no raw `reset --hard`/`git clean` (good — matches the "remove reset --hard and clean" requirement). Status: implemented_but_incomplete — tool class + governance/catalog entries exist, but CLI and MCP wiring to actually invoke it are missing, making `rush patch apply` and `rush_patch_apply` unusable end-to-end despite the plan calling this the "genuinely unimplemented" P64-04. This matches the task prompt's named drift item "rush patch apply unregistered" exactly.
- P64-05: implemented. src/rush/mcp_support/tool_registry.py:14,137,138 imports and calls `sanitize_value` at the shared MCP result egress point exactly as P64-05.2 requires. Status: implemented_but_unevidenced (evidence.md never mentions P64-05).
- P64-06: substantially implemented. src/rush/engines/{promptfoo,garak,deepeval,guardrails}.py all exist (3-4.6KB each); src/rush/tools/ai_eval.py has `required_permissions` (1 grep hit — needs deeper read to confirm gating order, not fully verified). Status: implemented_but_unevidenced, permission-gating depth not independently confirmed (budget-limited).
- P64-07: not independently checked (grep budget spent elsewhere); no strong evidence either way beyond it not appearing in evidence.md. Status: unknown/unevidenced — not verified this session.
- P64-08 (mutation): implemented. src/rush/tools/mutation.py:367-369 imports and calls `register_owned_artifact(root, report, "mutmut", permissions=permissions)` — the live mutmut-invocation branch described in P64-08.2 is present (28 grep hits for live/mutmut markers). Status: implemented_but_unevidenced — flatly contradicts evidence.md's "remain planned" claim.
- P64-09 (fuzz): implemented. src/rush/tools/fuzz.py:377,390,403 imports/calls `register_owned_artifact` for atheris crash + report artifacts (23 grep hits for atheris/live markers). Status: implemented_but_unevidenced.
- P64-10 (load): implemented. src/rush/tools/load.py:440,442 calls `register_owned_artifact(root, report, "k6", ...)` (28 grep hits for k6/live markers). Status: implemented_but_unevidenced.
- P64-11 (contract): implemented. src/rush/tools/contract.py:414,452 calls `register_owned_artifact` for pact reports (24 grep hits for pact-provider-verifier/live markers). Status: implemented_but_unevidenced.
- P64-12 (test-heal): partial signal only (4 grep hits for "Async Race Condition|Unseeded Random|Global State Leak|def diagnose" in src/rush/tools/test_heal.py) — the three named ADR-0034 causes ARE referenced in source. src/rush/tools/test_heal.py:331,353 show `runs: int = 20` (source default matches plan's "20 iterations... default"). But sync_docs live check shows `contracts.mcp.rush_test_heal.runs: default/required/type mismatch; expected default 20; recorded default 5` — i.e., the MCP-registered schema still advertises a stale default of 5 while the implementation already uses 20. Status: implemented_but_unevidenced for the diagnosis logic; the MCP contract record is stale (classified below).
- P64-13 (provenance_ai): signal present (7 grep hits for phi/n11 markers) — not deeply verified. Status: implemented_but_unevidenced (weak confidence, budget-limited).
- P64-14 (mem_profile/cold_start): signal present (15 grep hits for terminal_reason/peak_memory_bytes across both files). Status: implemented_but_unevidenced.
- P64-15 (db_drift_rules/db_drift/strictify): src/rush/tools/db_drift_rules.py exists (28.8KB, substantial). Not deep-verified beyond existence. Status: unknown/unevidenced.
- P64-16 (license_matrix/media_opt): 3 grep hits for license_expression/DOCTYPE/ENTITY markers — weak signal, not confirmed implemented. media_opt.py has 7 function defs but none named exactly "optimize"/"sanitize" (grep for those literal names returned 0) — function names differ from plan's literal description; needs a read to confirm actual behavior, not done this session. Status: unknown/unevidenced.
- P64-17 (iam_audit/coverage): 2 grep hits only — weak signal. Status: unknown/unevidenced.
- P64-18 (error_catalog/tree-sitter): pyproject.toml:29 confirms `tree-sitter-typescript==0.23.2` pin is present exactly as P64-18.2 requires. src/rush/tools/error_catalog.py has 5 hits for tree_sitter_typescript/_extract_ts_js_exceptions markers — dependency is pinned and the named function appears to reference the grammar. Status: implemented_but_unevidenced (moderate confidence).
- P64-19 (staged_scanner): src/rush/hook/staged_scanner.py and tests/test_staged_scan_bytes.py both exist. Not run live this session (budget). Status: files exist; unevidenced whether tests pass.
- P64-20 (checkov/CI gates): src/rush/engines/checkov.py has 1 hit for `file_abs_path` (the exact field named in P64-20.2's translator fix) — weak positive signal only. tests/test_ci_contract.py exists (2.2KB). Status: unknown/unevidenced, not deep-verified.

CAVEAT: Packets P64-07, P64-15, P64-16, P64-17, P64-20 were only lightly grep-sampled due to the scale of 21 packets vs. Scout's command budget; "unknown/unevidenced" for these means genuinely not verified this session, not "confirmed unimplemented." A deeper follow-up pass (or per-packet pytest runs) is needed before treating them as done or as gaps.

## Contract drift classification (live `uv run python scripts/sync_docs.py --check` output, this session)

Ran live; exit 1, 56 findings total (full raw list also visible via /tmp/sync_out.txt this session, not persisted).

- `rush ship clean`: missing parameter allow_artifact_write / missing parameter apply / stale parameter dry_run — CLASSIFY: stale_recorded_contract. src/rush/tools/ship/cleaner.py + P64-02 evidence (provisional but real) show `apply`/`allow_artifact_write` are the actual current parameters; the recorded CLI contract still expects the old `dry_run` name. Feature is implemented; the doc/registration record is stale.
- `rush test-heal`: missing allow_artifact_write/allow_build/allow_slow/dry_run/seed, plus `runs` default mismatch (expected 20, recorded 5) — CLASSIFY: stale_recorded_contract. src/rush/tools/test_heal.py:331,353 already default runs=20 and the ADR-0034 causes are implemented; the registered CLI contract has not been updated to the current permission-gated signature (matches P64-12.2's "Expose runs/seed/dry-run and runtime slow/artifact-write grants in CLI/MCP").
- `rush patch apply` unregistered (CLI) + `rush_patch_apply` unregistered (MCP) — CLASSIFY: missing_feature. Unlike the two above, this is not a stale doc/schema record of an existing wired command — grep confirms `patch_apply_cmd` in cli.py is never attached to a click group, and mcp.py has zero reference to PatchApplyTool/rush_patch_apply. The command is genuinely not invokable by an end user through either transport, even though the underlying `PatchApplyTool` class exists.
- MCP param mismatches for rush_benchmark, rush_cold_start, rush_contract, rush_fuzz, rush_load, rush_media_opt, rush_mem_profile, rush_mutation, rush_prompt_eval, rush_ship_clean, rush_tui_diff (~45 "missing parameter" lines) — CLASSIFY: stale_recorded_contract for rush_mutation, rush_fuzz, rush_load, rush_contract, rush_ship_clean specifically (source-verified: mutation.py/fuzz.py/load.py/contract.py already implement source_paths/test_paths/harness/corpus/script/target_url/pact_files/provider_url per the "Live workload input contract for P64-08-11" table in the plan, and cleaner.py already implements apply/allow_artifact_write) — the MCP registration schema simply hasn't been updated to expose params the tool functions already accept. For rush_benchmark, rush_cold_start, rush_media_opt, rush_mem_profile, rush_prompt_eval, rush_tui_diff: mixed signal — grep found some named params already present in source (rush_cold_start has `dynamic`/`timeout_seconds` markers: 23 hits; rush_mem_profile 21 hits; rush_tui_diff 16 hits; rush_prompt_eval 7 hits; rush_benchmark 10 hits) suggesting these are also stale_recorded_contract rather than missing_feature, but media_opt.py had 0 hits for literal `optimize`/`sanitize` function names (7 functions exist under different names) — NOT independently confirmed whether rush_media_opt's underlying capability is implemented; flag as unresolved, do not classify with confidence.
- `contracts.mcp: missing registered command rush_patch_apply` — duplicate of the CLI finding above; CLASSIFY: missing_feature.
- Stale doc SHA-256 / "historical body changed" entries (docs/CLI_REFERENCE.md, docs/MCP_REFERENCE.md, docs/SAFETY.md, docs/safety/permissions.md, docs/safety/safety-overview.md, docs/vibecoding/the-vibecoder-workflow.md, docs/phase-plans/phase-64-implementation-evidence.md, docs/.DS_Store, docs/KNOWN_ISSUES.md) plus ~9 "missing coverage receipt" entries under docs/goals/**, docs/developer/phase-63-66-coding-agent-handoff.md, docs/phase-plans/MC01.md — these are P64-00 checker findings unrelated to the named ~15-mismatch/patch-apply/ship-clean/test-heal set the task asked about; noting them because they mean P64-00's own acceptance is not currently green, not classifying them into missing/stale-contract buckets since they are not API contract drift.

## Real-engine acceptance gap (tests/test_executed_modes.py)

Confirmed live in this session (all 5 skips are exactly these, matching the task's "1696 passed, 5 skipped" baseline):

- `test_mutation_real_workload` (tests/test_executed_modes.py:323, gated via `_require_real_engine("mutmut")` at :248) — requires the `mutmut` CLI binary on PATH.
- `test_fuzz_real_workload` (tests/test_executed_modes.py:684, gated via `_require_real_module("atheris")` at :672) — requires the `atheris` Python module importable.
- `test_load_real_workload` (tests/test_executed_modes.py:1002) and `test_load_real_workload_clean_target` (tests/test_executed_modes.py:1059) — both gated via `_require_real_engine("k6")` at :248 — require the `k6` CLI binary on PATH.
- `test_contract_real_workload` (tests/test_executed_modes.py:1500) — gated via `_require_real_engine("pact-provider-verifier")` — requires the `pact-provider-verifier` CLI binary on PATH.

Live check this session: `which mutmut k6 pact-provider-verifier` -> all three "not found"; `uv run python -c "import atheris"` -> `ModuleNotFoundError: No module named 'atheris'`. Ran `RUSH_REQUIRE_REAL_ENGINES=1 uv run python -m pytest tests/test_executed_modes.py -q -k "real_workload or required_engine_absence or required_atheris_module_absence"` -> 5 failed (the real_workload tests now hard-fail with `AssertionError: required engine absent: <name>` instead of skipping, confirming the gating helper works as designed), 2 passed (the two "required_*_absence" negative tests, which assert failure is correct).

None of the four engines are blocked by credentials or a network service at test-execution time — the plan's own section "Live workload input contract for P64-08-11" and its CI section (plan lines 111-115) name them as pip-installable/binary-downloadable dev-only dependencies (`uv pip install mutmut==3.7.0 atheris==3.1.0 pact-python-cli==2.6.0.1`; k6 v2.2.0 from its official release archive with SHA-256 verification), installed only in the CI `engine-contracts` job on `ubuntu-24.04`, never as a Rush runtime dependency. The blocker in this environment is purely that those install steps have not been run locally — not a hard external requirement (no license key, no live third-party service call needed to just run these workloads against local fixtures). k6's download does require network access to GitHub releases; mutmut/atheris/pact-python-cli are installable via `uv pip install` if network/PyPI access is available in this sandbox (not tested this session — installing anything is out of scope for a read-only Scout).
