# Decisions

Durable architecture choices live in [Maintainer ADRs](maintainers/adr/README.md). The implementation program's detailed ADRs live in [`adr/`](adr/README.md). Major decisions include stdio-only MCP, optional engine discovery, catalog-driven metadata, explicit safety gates, fixture-first adapters, bounded CI, and Git-root-bounded configuration.
