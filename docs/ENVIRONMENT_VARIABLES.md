# Environment variables

See the authoritative [Environment variable reference](reference/environment-variables.md). Rush directly uses `RUSH_LOG_LEVEL` and detects `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` only in the no-call review stub. `PATH`, `VIRTUAL_ENV`, and `PYTHONPATH` affect executable/import discovery.
