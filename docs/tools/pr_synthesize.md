# Semantic PR Card Synthesizer (`rush pr-synthesize`)

## Overview
`rush pr-synthesize` aggregates Git diff statistics, changed file classifications, and quality/security verification evidence into a structured, production-ready GitHub Pull Request description card. Exporting the card to a file is permission-gated under `--allow-artifact-write`.

## Classification
- **Category**: `workflow`
- **Maturity**: `real_adapter`
- **Tool Name**: `pr-synthesize`
- **FastMCP Tool**: `rush_pr_synthesize`

## Capabilities
0. **Risk Tiering & Assessment**: Computes PR risk (`LOW`, `MEDIUM`, `HIGH`) based on net churn (>500 lines or failing evidence -> `HIGH`, >100 lines or warning -> `MEDIUM`, otherwise `LOW`).
0.1. **CODEOWNERS Reviewer Routing**: Discovers `.github/CODEOWNERS`, `CODEOWNERS`, or `docs/CODEOWNERS`, parses file patterns, and automatically populates recommended reviewers.
1. **Diff Statistics & Change Breakdown**: Analyzes `git diff --stat` and `git diff --numstat` against a target base branch (default: `main`), extracting file counts, insertions, and deletions, and categorizing modified paths into source, tests, documentation, and configuration.
2. **Quality & Security Gate Summary**: Formats verified platform gates into the PR description:
   - Build provenance draft status (in-toto Statement v1).
   - Clean architecture layer boundary compliance.
   - Blast radius impact summary.
   - Test suite and FastMCP schema parity.
3. **Contained File Export**: Exports the generated PR markdown card to a specified destination path. Writing to disk requires explicit `--allow-artifact-write` permission and verifies workspace path containment.

## CLI Usage

### Basic Synthesis (Against main)
```bash
rush pr-synthesize .
```

### Synthesis Against Specific Base Branch
```bash
rush pr-synthesize . --base-ref origin/main
```

### JSON Wire Format
```bash
rush pr-synthesize . --json
```

### Export PR Card to File
```bash
rush pr-synthesize . --export-path .rush/PR_CARD.md --allow-artifact-write
```

## FastMCP Usage
```json
{
  "name": "rush_pr_synthesize",
  "arguments": {
    "path": ".",
    "base_ref": "main",
    "allow_artifact_write": false
  }
}
```

## Canonical Result Schema
```json
{
  "tool": "pr-synthesize",
  "engine": "git-diff-synthesizer",
  "engine_version": "1.0.0",
  "status": "ok",
  "duration_ms": 35,
  "summary": "pr-synthesize: synthesized PR card against 'main' (4 files changed, +120/-15)",
  "findings": [],
  "raw": {
    "pr_card": "# Pull Request: Platform Innovation Enhancements\n\n## Summary\nAutomated semantic PR description generated by Rush CLI v0.3.0.\n\n...",
    "diff_stat": " src/rush/tools/error_catalog.py | 120 +++++++++++++++++\n 1 file changed, 120 insertions(+)\n",
    "changed_files": [
      {
        "path": "src/rush/tools/error_catalog.py",
        "insertions": 120,
        "deletions": 0
      }
    ]
  },
  "metadata": {
    "base_ref": "main",
    "files_changed": 1,
    "insertions": 120,
    "deletions": 0
  }
}
```
### Provenance Integration (Phase 59)
PR synthesizer includes truthful build provenance status and checksums in generated PR cards.
